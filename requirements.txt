# --- Core (required to run the MVP offline) ---
pydantic>=2.6,<3.0

# --- Ingestion ---
httpx>=0.27
trafilatura>=1.8
pdfplumber>=0.11
feedparser>=6.0

# --- LLM (optional at runtime: pipeline falls back to the offline extractor
#     when no ANTHROPIC_API_KEY is set) ---
anthropic>=0.40

# --- UI (optional; only needed for `streamlit run mci/app.py`) ---
streamlit>=1.36

# --- Committee export (optional; Markdown export needs none of these) ---
python-docx>=1.1
python-pptx>=0.6
openpyxl>=3.1


# --- Dev / test ---
pytest>=8.0
